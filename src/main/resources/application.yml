spring:
  application:
    name: "pjinsight"
  datasource:
    url: "jdbc:postgresql://localhost:5432/pjinsight?useTimezone=true&serverTimezone=UTC"
    username: "postgres"
    password: "@Senha123"
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
    show-sql: false
    open-in-view: false