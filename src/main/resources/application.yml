server:
  port: ${PORT:8080}

spring:
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://ep-red-unit-ad718262-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require&channelBinding=require}
    username: ${DATABASE_USERNAME:neondb_owner}
    password: ${DATABASE_PASSWORD:npg_w0OBoDYizb9J}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    com.zaxxer.hikari: DEBUG

api:
  security:
    token:
      secret: ${JWT_SECRET:pjinsights1234567}

aws:
  api:
    profile-classifier:
      url: ${PROFILE_CLASSIFIER_URL:https://1r4rllo8ff.execute-api.sa-east-1.amazonaws.com/Prod}

ai:
  model: ${AI_MODEL:gemini-1.5-flash}
  apikey: ${AI_API_KEY:AIzaSyCAtbbqFJ-bV-E56u8eWyc6qtMmMClOchI}